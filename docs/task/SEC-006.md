# SEC-006 CI/CD 安全加固

- **status**: pending
- **priority**: P1
- **owner**: (未分配)
- **createdAt**: 2026-04-17 10:00

## 描述

加固 GitHub Actions 工作流的安全性。

验收标准：
1. 所有工作流输入（`inputs.base_url` 等）在 shell 使用前做 URL 格式校验
2. 解码 secrets 后使用 `::add-mask::` 防止日志泄露
3. 审查并最小化 Actions 权限范围

## 进行时描述

正在加固 CI/CD 安全

## 依赖

- **blocked by**: (无)
- **blocks**: (无)

## 笔记

涉及文件：
- `.github/workflows/build-ios.yaml:298` — sed 命令注入风险
- `.github/workflows/build-macos.yaml` — secrets 日志泄露
- `.github/workflows/build-windows.yaml` — 同上
- `.github/workflows/build-server.yaml:14-16` — permissions 范围
