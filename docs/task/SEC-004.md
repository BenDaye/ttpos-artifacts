# SEC-004 请求安全限制

- **status**: pending
- **priority**: P1
- **owner**: (未分配)
- **createdAt**: 2026-04-17 10:00

## 描述

为 Server 端添加请求层面的安全限制。

验收标准：
1. Gin router 设置 `MaxMultipartMemory`（建议 100MB）
2. 分页参数 `limit` 添加上限校验（建议 max=1000）
3. 错误响应中移除内部细节（`err.Error()` 不直接返回客户端），仅记日志

## 进行时描述

正在添加请求安全限制

## 依赖

- **blocked by**: (无)
- **blocks**: (无)

## 笔记

涉及文件：
- `server/server/server.go` — MaxMultipartMemory 配置
- `server/server/handler/catalog/get.go:52-64` — 分页上限
- `server/server/handler/delete/delete.go:53` 等多处 — 错误信息脱敏
