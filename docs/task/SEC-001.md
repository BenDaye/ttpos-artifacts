# SEC-001 凭证安全加固

- **status**: pending
- **priority**: P0
- **owner**: (未分配)
- **createdAt**: 2026-04-17 10:00

## 描述

清理项目中所有泄露的凭证和不安全的密钥存储方式。

验收标准：
1. `server/.env.example` 中所有真实凭证替换为占位符
2. `deploy/docker-compose.yml` 移除 `changeme` 默认密码，改为必填
3. Redis 配置添加密码认证（`--requirepass`）
4. `server/server/server.go:49-50` 的 `os.Setenv` 改为依赖注入
5. `deploy/docker-compose.yml:9` 的 GCS 凭证文件挂载改为 Docker Secrets

## 进行时描述

正在加固凭证安全配置

## 依赖

- **blocked by**: (无)
- **blocks**: (无)

## 笔记

涉及文件：
- `server/.env.example:4,5,7,16-17`
- `deploy/docker-compose.yml:9,50,65`
- `server/server/server.go:49-50`
- `deploy/.env.example`
