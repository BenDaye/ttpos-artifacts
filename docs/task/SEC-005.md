# SEC-005 HTTP 与前端安全加固

- **status**: pending
- **priority**: P1
- **owner**: (未分配)
- **createdAt**: 2026-04-17 10:00

## 描述

加固 HTTP 传输层和前端渲染安全。

验收标准：
1. `dashboard/nginx.conf` 添加安全响应头（CSP、X-Frame-Options、HSTS、X-Content-Type-Options、Referrer-Policy）
2. ReactMarkdown 渲染 Changelog 时集成 DOMPurify 消毒
3. `dashboard/src/utils/clipboard.ts` 的 `innerHTML` 改为 `textContent`
4. `server/.env.example` 中移除 Chrome 扩展的 CORS 白名单

## 进行时描述

正在加固 HTTP 和前端安全

## 依赖

- **blocked by**: (无)
- **blocks**: (无)

## 笔记

涉及文件：
- `dashboard/nginx.conf` — 安全头
- `dashboard/src/components/Dashboard.tsx:875` — Markdown 渲染
- `dashboard/src/components/ChangelogModal.tsx:37` — Markdown 渲染
- `dashboard/src/utils/clipboard.ts:74,79` — innerHTML
- `server/.env.example:2` — CORS 配置
