# ENH-010: 为元数据模型引入持久化 Sort 排序与 Dashboard 拖拽

- **status**: completed
- **priority**: P2
- **owner**: ben
- **createdAt**: 2026-06-01
- **completedAt**: 2026-06-01
- **related**: SEC-007

## 描述

App / Channel / Platform / Arch 四个 owner 维度元数据列表此前没有任何排序概念，展示顺序取决于插入/`_id` 顺序，用户无法自定义。本任务为这四类引入一个持久化的 `Sort` 字段，并在 Dashboard 提供拖拽排序，让每个 owner 能自定义这些实体的显示顺序。

## 方案

采用**自愈式 dense-integer**（无独立数据迁移）：

- `Sort` 按 **(owner, type)** 作用域。四类同住 `apps_meta` collection，靠判别字段区分（`channel_name` / `platform_name` / `arch_id` / `app_name`），绝不取全局 max，避免四类交错。
- 读取：列表查询统一按 `{sort:1, _id:1}` 升序。老文档没有 `sort` 字段，Mongo 升序里 null/缺失排在数字之前，因此存量维持当前创建顺序，直到第一次拖拽。
- 新建：在创建收口按 (owner, type) 取现有 `sort` 最大值 +1（无则 0），追加到末尾。
- 重排：每类一个 `POST /{type}/reorder` 端点，前端传**完整有序 ID 列表**，后端校验全部属于该 owner+type 后用 bulkWrite 重写 `sort = 0..N-1`。复用既有 `CheckPermission(PermissionEdit, Resource{Type})`，不新造权限。

## 范围

### Server
- `server/server/model/model.go`：App/Channel/Platform/Arch 各加 `Sort int`（`bson:"sort" json:"Sort"`）。
- `server/mongod/list.go`：`listItems`（四列表唯一收口）加 `SetSort({sort:1,_id:1})`。
- `server/mongod/create.go`：`createMetaDocument` 注入按 (owner,type) 的默认 `sort = max+1`；新增纯函数 `metaDiscriminatorField` / `nextSortValue` + DB 薄层 `maxMetaSort`。
- `server/mongod/reorder.go`（新增）：四个 `Reorder*` 仓储方法、纯校验函数 `computeReorderAssignments`、sentinel error、DB 薄层 `reorderMeta`。
- `server/server/handler/catalog/reorder.go`（新增）：四个 reorder handler，经窄接口 `metaReorderer` 断言取得仓储能力。
- `server/server/handler/handler.go`：`AppHandler` 接口 + `appHandler` 转发四个 Reorder。
- `server/server/server.go`：注册 `POST /{channel,platform,arch,app}/reorder`。
- `server/server/model/model_test.go`：四个 JSON 契约 want 追加 `Sort`。

> 关键决策：`Reorder*` **刻意不并入** `mongod.AppRepository` 大接口，仅作为具体 `*appRepository` 能力，由 catalog 窄接口 `metaReorderer` 断言获取。这样 TUF bootstrap 的测试 mock 无需被动新增方法即可继续满足 `AppRepository`，**零 TUF 文件改动**。

### Dashboard
- `packages/shared/src/index.ts`：`AppSummary`/`Channel`/`Platform`/`Architecture` 各加 `Sort: number`。
- 依赖：`@dnd-kit/core` / `@dnd-kit/sortable` / `@dnd-kit/utilities`。
- `apps/web/src/shared/components/common/sortable-list.tsx`（新增）：基于 dnd-kit 的可复用排序原语（无障碍交给 dnd-kit）。
- `apps/web/src/shared/lib/reorder.ts`（新增）：`reorderById` 纯函数。
- `features/{channels,platforms,architectures,apps}/{api.ts,hooks.ts}`：各加 `reorder(ids)` 与 `useReorder*Mutation`（乐观更新 + 回滚 + invalidate）。
- 四个列表 UI 接入拖拽手柄；搜索/过滤激活时禁用拖拽，避免对子集重排。apps 落在 list 视图，card/board 按 `Sort` 显示默认序。
- `apps/web/src/features/channels/hooks.test.tsx`（新增）：reorder hook 乐观更新/回滚单测。
- `dashboard-next/.gitignore`：补 `.omc/`（与根仓库策略一致，避免 OMC 运行态产物被 `eslint .` 纳入）。

## 验收标准

1. 四模型有 `Sort` 字段且对外 JSON 暴露 `Sort`；契约测试同步。
2. 四列表读取按 `sort` 升序；老数据维持创建顺序。
3. 新建实体默认追加到该 owner+type 末尾。
4. 每类 reorder 端点按 (owner,type) 全量重写，拒绝外来/未知/非法/重复 id，复用 Edit 权限。
5. 前端四列表可拖拽排序，乐观更新 + 失败回滚，搜索时禁用。
6. 视觉值走 DESIGN.md token，无新增 hex/arbitrary；文案 brand-neutral；不碰 TUF。

## 验证

本地工具链 go1.25.5；dashboard-next 用 bun。

- `go build ./...` → 通过。
- `go vet ./...` → 通过。
- `go test ./...` → 除根包 `faynoSync`（E2E，需 `.env` + mongo，本地缺省 `panic: Config File ".env" Not Found`）外全部 `ok`，含 `faynoSync/server/tuf/bootstrap`、`faynoSync/server/model`、`faynoSync/mongod`。
- `gofmt -l`（改动文件）→ 空。
- `bun run typecheck` → 通过。
- `bun run lint` → 0 error（45 warning 全为既有，非本次引入）。
- `bun run test` → 通过（含新增 reorder hook 用例）。
- `bun run build` → 成功。

## 验证缺口（需 vm-node02 / 有 mongo 环境验证）

> 2026-06-02 已在 vm-node02 实测闭合，见下文「阶段一实测结果」。

- 根包 E2E `faynoSync_test.go` 与所有依赖 live mongo 的路径（`maxMetaSort` / `reorderMeta` / `resolveOwnerScope` 的真实读写、`listItems` 新排序对四列表的实际效果、老文档靠 `_id` tiebreaker 维持创建顺序）本地无法验证。
- 载荷假设「Mongo 升序中 null/缺失 < 数字」需在 staging 实测确认。
- 拖拽交互的浏览器端到端（真实数据 + 后端）未做冒烟。

## 已知边界（非阻塞）

- 元数据列表服务端固定 cap 100（既有限制）；某 owner 资源 > 100 时列表与 reorder 仅覆盖前 100，其余 `sort` 不变。
- team-user 若仅可见其 `Allowed` 子集，对子集 reorder 会与 admin 其它同类资源的 sort 交错；建议后续按需收紧（如 reorder 限 admin 或按 Allowed 校验完整集合）。
- create 默认 `sort = max+1` 在并发创建下有极小竞态，`_id` tiebreaker 保证确定顺序，低频管理操作可接受。
- reorder 对 `len(ids) <= 1` 直接 no-op：单元素请求即便含非法 hex 或外来 id 也返回 200（跳过输入校验），对数据无害仅 API 契约略松。
- `asReorderer` 为每请求运行时断言（失败 500）；功能正确，可后续优化为 `NewAppHandler` 构造期一次性断言（fail-fast）。

## 笔记

- 2026-06-01：方案与用户确认（持久化排序字段 / 四模型 / 拖拽 / 方案 A）。后端、前端两路并行实现。
- 2026-06-01：修复后端扩展接口导致 TUF bootstrap mock 失配的回归——改为窄接口断言，零 TUF 文件改动；修复 apps list 行 `onKeyDown` 抢占手柄键盘拖拽（忽略子元素来源事件）；将 `.omc/` 补入 dashboard-next gitignore 使 `eslint .` 跳过 harness 产物。
- 2026-06-02：边界变更——本任务原范围中「apps 落在 list 视图，card/board 按 `Sort` 显示默认序」已扩展：apps 的 **card 与 board(column) 视图现也支持拖拽重排**，与 channels/platforms/architectures 的 card 拖拽对齐，复用 `useReorderAppsMutation`（乐观更新+回滚）。同时把 board 列头由单一 `<button>` 重构为「非交互容器 + grip / 可点击区 / 操作区」三同级结构，消除既有 button-in-button 嵌套违规，grip 统一为原生 `<button>`。验证：`typecheck` / `lint`(0 error) / `test`（含新增 card/board grip 接线组件测试）/ `build` 通过；`playwright`（applications + app-detail）全绿，含 card/board 拖拽 POST 断言、card 拖拽 stateful-mock 的 DOM 持久重排断言、列头重构后既有导航用例（改用 `board-column-open` testid）不回归。

## 生产上线验证与切换清单

> 用途：闭合上文「验证缺口」，在镜像更新到承载真实数据的环境前给出可执行核对步骤。
> 结论前提（已逐行核对代码确认）：**本变更无任何数据迁移**，新镜像对老数据靠「自愈式」向后兼容，不在启动时改写存量文档。`migrations/` 最新仅到 `20260417100000_create_owner_index_on_apps`，无 sort 回填文件，这是预期设计。

### 决策记录

- **保持自愈式、不新增回填迁移**（2026-06-02 与用户确认）。理由：自愈方案已覆盖老数据，临镜像切换前新增迁移代码反而引入想规避的风险。
- 代码层逐项核对（仅静态分析，未跑 live mongo）：
  - `list.go` 按 `{sort:1,_id:1}`：老文档缺 `sort` → Mongo 升序里 null/缺失 < 数字 → 老文档整体在前、内部按 `_id` 维持创建序。
  - `create.go` `maxMetaSort` 过滤 `sort:{$exists:true}`：**绝不把 null 误当 0**，新文档拿 dense `max+1`（该 scope 全无数字 sort 时为 0）。
  - `reorder.go`：全作用域 `$set sort=0..N-1`，拖一次即把该 scope 全部治愈为连续整数。
  - `update.go`：四类 `Update*` 均定向 `$set`、非整文档替换 → 重命名/改 logo 不触碰 `sort`，字段保留。
  - 前端：`reorderById` 按 id 列表重排；`disabled={isFiltering}` 保证只对**完整作用域**重排，杜绝子集污染。

### 关键缺口（决定「确保无误」成败）

迁移风险**只存在于没有 `sort` 字段的老文档上**。若验证环境的数据全是 ENH-010 之后创建的（条条带 `sort`），任何验证都是**假绿灯**。因此 staging 验证必须先人为构造无 `sort` 老文档。

### 阶段一：vm-node02（staging）真验证

1. **构造无 `sort` 老文档**：挑一个 ≥3 项的 `(owner, type)` 作用域，`$unset` 掉 `sort` 模拟存量。库名/认证以 compose 的 env 为准（`db` service），命令模板：
   ```bash
   cd /home/ben/projects/ttpos-artifacts && aissh exec 8b154be3-53ce-4b26-bc09-6752f8cfd499 \
     "cd /home/ubuntu/ttpos-releases && docker compose exec -T db mongosh <db> --quiet --eval '
       db.apps_meta.updateMany({ channel_name:{\$exists:true}, owner:\"<owner>\" }, { \$unset:{ sort:\"\" } })'" \
     --reason="ENH-010 staging：构造无 sort 老文档"
   ```
2. **部署新镜像到 staging**（CI 推完镜像后）：
   ```bash
   gh run watch <build-server-run-id> --exit-status
   cd /home/ben/projects/ttpos-artifacts && aissh exec 8b154be3-53ce-4b26-bc09-6752f8cfd499 \
     "cd /home/ubuntu/ttpos-releases && docker compose pull api dashboard && docker compose up -d api dashboard" \
     --reason="ENH-010 staging 验证：部署 sort 改动"
   ```
3. **四项断言**：
   - ① **老文档维持创建序**：`GET /channel/list` 返回那批被 `$unset` 的老文档，顺序 = 创建顺序（按 `_id` 升序）。
   - ② **新建落末尾 + sort 正确**：在该 scope 新建一个 channel，列表中出现在最后；DB 里其 `sort` 为该 scope 现有数字 sort 的 `max+1`（若之前全无数字 sort 则为 0）——即 `maxMetaSort` 的 `$exists` 守卫确实生效、没把 null 当 0。
   - ③ **拖拽写 dense**：在 dashboard 拖一次，确认 `POST /channel/reorder` 发出；DB 里该 scope 全部 `sort` 变成连续 `0..N-1`。
   - ④ **老+新混合治愈**：拖一个含老文档的顺序，确认之前 `$unset` 的老文档现在都有了数字 `sort`。
4. **DB 直查辅助**（可直观看到混合态 null 在前、数字在后）：
   ```bash
   docker compose exec -T db mongosh <db> --quiet --eval '
     db.apps_meta.find({ channel_name:{$exists:true}, owner:"<owner>" }, { channel_name:1, sort:1 }).sort({ sort:1, _id:1 })'
   ```

### 阶段一实测结果（2026-06-02，vm-node02）

当前 vm-node02 运行的 api 镜像已含 sort/reorder（`POST /channel/reorder` 返回 401≠404），server 代码与 main HEAD 一致，无需重新部署即可验证。channel 作用域（owner=ttpos）天然有 3 个无 `sort` 老文档（prod/test/dev），为真实存量样本，**无需 `$unset`**。

- **核心载荷假设（null/缺失 < 数字）**：临时集合探针 `[{n:1},{n:2,sort:0},{n:3},{n:4,sort:5},{n:5,sort:1}]` 按 `{sort:1,_id:1}` 排序输出 `1,3,2,5,4`——无 sort 的 n=1/n=3 在前并按 `_id`，再 sort 升序 0/1/5。✅ 地基成立。
- **① 老文档维持创建序**：拖前 channel 列表 `prod/test/dev` 按 `_id`（非字母序），= 创建序。✅
- **③ reorder 写 dense**：dashboard 拖一次后，channel 全部 `sort = 0,1,2,3` 连续。✅
- **④ 老+新混合治愈**：`ch_nosort` 由 3 → 0，原 prod/test/dev 三个老文档全部获得数字 sort。✅
- **② 新建落末尾**：新建 channel `zzz` 拿到 `sort=3`（= 拖后现有 max 2 +1），落在末尾，证 `maxMetaSort` 的 `$exists` 守卫生效、未把 null 当 0。✅

结论：self-healing 在真实 mongo + 真实 create/reorder 代码路径上四项断言全绿，上文「验证缺口」已闭合。阶段二（生产镜像切换）因 vm-node02 即测试环境、且镜像已运行同款 server 代码，本任务范围内不再单独执行。

### 阶段二：生产镜像切换（用户执行）

> memory 记录截至 2026-06-01 仅有 vm-node02(staging)、尚无生产；下列步骤与环境无关，prod 具体编排命令以你的实际环境为准。

1. **切换前备份** `apps_meta`（`mongodump` 或导出）。自愈方案不改老数据，备份只是廉价保险。
2. **阶段一四项断言全绿后**再切 prod：拉新 `api`（与 `dashboard` 锁步，二者版本须匹配——`reorder` 端点与前端拖拽要同源）。
3. **切换后烟雾验证**：`GET /{channel,platform,arch,app}/list` 各端点 200、老数据维持创建序；在 prod dashboard 抽查一次拖拽重排生效并刷新后持久。
4. **回滚预案（安全）**：自愈方案对老数据**零写入**（仅 create/reorder 写 `sort`）。回退到旧镜像后，旧 `listItems` 无 `SetSort`、旧 JSON 模型无 `Sort` 字段，会**直接忽略**已写入的 `sort`（无害冗余字段，不报错、不破坏契约），按旧自然序展示。前端旧版亦不调用 `reorder`。故 api+dashboard 锁步回滚是干净的。
